#include <iostream>
#include "Card.h"

int main()
{
	srand(time(NULL));
	//Card card;
	//Card card("Community Chest");
	
	//card.initialiseCards();
	
	/*
	card.selectCard(0);
	
	card.selectCard(1);
	
	card.selectCard(0);
	
	card.selectCard(1);
	
	*/
	
	return 0;
}

